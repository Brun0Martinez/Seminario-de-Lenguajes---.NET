using System.Numerics;
using System;
double [,] matriz = new double[,]
}
{3.2,2.3,4.4},
{5.3,1.2,2.2},
{1.2,3.2,3.2},
};
double[][] GetArregloDeArreglo(double[,] matriz)
{
double [][] arregloDeArreglo = { new double[matriz.GetLength(0)], new double[matriz.GetLength(1)] };;
for(int i = 0; i < matriz.GetLength(0); i++)
{
for(int j=0;j< matriz.GetLength(1); j++)
{
arregloDeArreglo[i][j]=matriz[i,j];
}
}
return arregloDeArreglo;
}
double [][] arreglo= GetArregloDeArreglo(matriz);
