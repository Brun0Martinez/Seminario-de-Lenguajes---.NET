6) Modificar la definición de la clase Matriz realizada en la práctica 4. Eliminar los métodos
SetElemento(...) y GetElemento(...). Definir un indizador adecuado para leer y escribir
elementos de la matriz. Eliminar los métodos GetDiagonalPrincipal() y
GetDiagonalSecundaria() reemplazándolos por las propiedades de sólo lectura
DiagonalPrincipal y DiagonalSecundaria.

  public double this[int fila, int columna]
    {
        get
        {
            return matriz[fila,columna];
        }
        set
        {
            matriz[fila,columna]=value;
        }
    }

 public double[] DiagonalPrincipal
    {
        get
        {
            double [] vector = new double [this.filas];
            int n = this.filas;
            for(int i=0; i<n; i++)
            {
                vector[i] = matriz[i,i];
            }
            return vector;
        }
    }
 public double[] DiagonalSecundaria
    {
        get
        {
             double[] vector = new double[this.filas];
            int n=this.filas;
            for(int i = 0; i < n; i++)
            {
                vector[i]=matriz[i,n-1-i];
            }
            return vector;
        }
    }
